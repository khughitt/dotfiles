if [ ! -f /etc/lsb-release ] || ! grep -q Ubuntu /etc/lsb-release; then
  return
fi

# ubuntu likes to be different..
alias fd="fdfind"
alias bat='batcat'
alias cat='batcat --wrap=never --paging=never'

# vi:syntax=bash
