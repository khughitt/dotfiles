# open db
.open <path>

# list tables
.tables

# describe table
.schema tbl

# number of days ince Nov 24, 4714 BC
julianday(now)
julianday(field)

# difference in days..
abs(julianday(now) - julianday(field))

# extract table / query results as JSON
sqlite3 x.db \
  -cmd ".mode json" "SELECT * FROM tbl;" |\
  python3 -m json.tool \
  > out.json
