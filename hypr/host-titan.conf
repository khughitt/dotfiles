# host-titan.conf — titan (desktop, 4K, NVIDIA GPU)

# GPU: NVIDIA
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia

# 4K: generous spacing
general {
    gaps_in = 12
    gaps_out = 12
}

decoration {
    rounding = 14
    blur {
        size = 10
        passes = 2
    }
}
