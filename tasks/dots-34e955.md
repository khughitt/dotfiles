---
id: dots-34e955
title: "work-link: surface the quiet skips (target without Cargo.toml, unchecked realpath in the lock loop, prunable entries flagged by git, --root under --ensure)"
status: done
priority: 3
size: s
complexity: low
process: direct
owner: main
created: 2026-09-15T02:15:00Z
updated: 2026-09-17T11:44:17Z
started: 2026-09-17T11:38:04Z
completed: 2026-09-17T11:44:17Z
depends: []
tags: [git]
agent: claude-code
---

Final review of ops-0f5ed2, minors: an external target whose checkout has no Cargo.toml is skipped silently (neither orphan nor reported); real=$(realpath -- "$child") in work_link_lock_worktrees is unchecked and an empty value skips the lock silently; prunable detection tests only path resolution, so an entry git itself marks prunable (broken .git file) is missed, and a failing git worktree list reads as an empty list; --root is parsed but ignored under --ensure. Each wants a report line or a refusal plus a test in tests/work_link.zsh.

## Notes

- 2026-09-17T11:44:17Z (dots-34e955): work-link now reports an external target with no Cargo.toml as an orphan, checks the lock loop's realpath and reports its failure, takes prunable detection from git worktree list's own prunable marks (with a failing listing reported instead of read as empty), and honors --root under --ensure; each with a test in tests/work_link.zsh
