# appearance
<ctrl-shift-+>  increase gui scaling
<ctrl-shift-->  decrease gui scaling

# transport
f9   toggle record

pgup    prev track
pgdn    next track

# views
<shift-tab>     toggle arrangement / editor
o               toggle arranger
a               automation
t               focus track

# grid (patching modifiers)
<ctrl-drag>     add with existing source
<alt-drag>      mix with existing source
<shift-drag>    blend with existing source
<ctrl-shift>    multiply

# note colors
# right click a blank area of editor

# file management
cntl + alt + s  collect & save

# piano roll
f           switch between drum view / piano
<shift-e>   toggle note expressions

# show multiple tracks together
# 1. in bottom left TRACK/CLIP section, choose "TRACK"
#   - select the "TRACKS" tab
# 2. cntl-click the tracks to display

# devices
?           popup browser

# solo
alt  + s       toggle track solo
<shift-click>  exclusive solo

# panel focus
cntl + k    toggle device window
ctrl + p    toggle project panel
d           focus device panel
alt + d     toggle device panel

# arranger
cntl + l   loop selected region
r          toggle arranger loop

# editor
ctrl + j    consolidate
shift + d   double content
g           make legato

# function keys
f2          toggle automation

# notes
<alt-1>     octave down
<alt-2>     octave up

<alt-q>     quantize dialog

<alt-r>         reverse
<shift-alt-r>   reverse pattern

# custom
<shift-alt-a>   toggle automation for all tracks

# looping
<ctl-l> / r     loop selected region (select _track_ region)

# record multiple takes 
# "play" -> "post-recording action" -> "record into next.."

# colors (missing audio samples indicators)
yellow - sample is outside of project
red - sample is missing
