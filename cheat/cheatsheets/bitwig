# appearance
<ctrl-shift-+>  increase gui scaling
<ctrl-shift-->  decrease gui scaling

# general
consolidate   ctrl-j
cut time      ctrl-shift-x

toggle browser panel  alt-b

make legato   g
quantize...   alt-q

reverse           alt-r
reverse pattern   shift-alt-r 

scale...    ctrl-`

toggle micopitch editing     shift-alt-m

toggle adaptive beat grid   /
toggle snap                 
toggle snap to events       shift-/
toggle snap to grid         shift-,
toggle snap to grid offset  shift-.

toggle undocked view        ctrl-p

# piano roll
f           switch between drum view / piano
<shift-e>   toggle note expressions

# show multiple tracks together
# 1. in bottom left TRACK/CLIP section, choose "TRACK"
#   - select the "TRACKS" tab
# 2. cntl-click the tracks to display

# record multiple takes 
# "play" -> "post-recording action" -> "record into next.."

# grid (patching modifiers)
<ctrl-drag>     add with existing source
<alt-drag>      mix with existing source
<shift-drag>    blend with existing source
<ctrl-shift>    multiply

