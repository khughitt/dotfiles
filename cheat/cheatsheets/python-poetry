# initialize from existing project
poetry init
poetry install

# add development dependency
poetry add -G dev ..

# launch shell
poetry shell

# install deps
poetry install

# list packages
poetry show
