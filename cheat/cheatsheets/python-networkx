# basics
G.adj
G.edges
G.nodes

# creation
nx.from_numpy_array(arr)
nx.from_scipy_sparse_array(arr)
nx.from_pandas_adjacency(df)

# force-directed layout
pos = nx.spring_layout(G, seed=321)

# plotting
nx.draw(G)
nx.draw(G, pos=pos)
nx.draw_networkx_labels(G, pos, labels)   # "labels" = dict of labels indexed by node

# vi:syntax=python
