# Configuration for kernel-gate. Machine-specific paths live here, not in the
# script. Installed to /etc/kernel-gate.conf.

# Checkout whose CERTIFIED_ALLOWLIST kernel_identifier is compared against the
# running kernel, to report whether the durability tuple is still certified.
# Leave empty on machines without that checkout; the check is then skipped.
ATOMS_REPO="${HOME}/d/atoms"
